#include<stdio.h>
#include<conio.h>
int
int main()
{
    int t,i,j;
    scanf("%d",&t);
    int a[t+1],b[101];

    for(i=1;i<=t;i++)
    {
        scanf("%d",&a[t]);
        for(j=1;j<=a[t];j++)
        {
            scanf("%d",&b[j]);
        }
        for(i=1;i<=((a[t]+1)/2);i++)
    {
        if(b[i]==i);
        else
        {
           k=0;
        }
    }
    for(i=((a[t]+1)/2)+1;i<=a[t];i++)
    {
        if(b[i]==(a[t]-i+1));
        else
        {
           k=0;
        }

    }
    }

}
